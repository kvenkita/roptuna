% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pruner-patient.R
\name{patient_pruner}
\alias{patient_pruner}
\title{Create a patient pruner}
\usage{
patient_pruner(wrapped_pruner, patience, min_delta = 0)
}
\arguments{
\item{wrapped_pruner}{A pruner object (e.g. \code{median_pruner()}).}

\item{patience}{Number of recent steps with no improvement required before pruning.}

\item{min_delta}{Minimum improvement threshold (default 0).}
}
\value{
A \code{PatientPruner} R6 object.
}
\description{
Wraps another pruner and only prunes when the wrapped pruner has been
recommending pruning AND there has been no improvement of at least
\code{min_delta} over the last \code{patience} reported steps.
}
\examples{
pruner <- patient_pruner(median_pruner(), patience = 3L)
study  <- create_study(pruner = pruner)
}
