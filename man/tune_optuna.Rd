% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-tune.R
\name{tune_optuna}
\alias{tune_optuna}
\title{Run a tidymodels workflow using roptuna for hyperparameter search}
\usage{
tune_optuna(
  object,
  resamples,
  suggest_fn,
  n_trials = 20,
  direction = "minimize",
  sampler = tpe_sampler(),
  pruner = NULL,
  storage = NULL,
  study_name = NULL,
  metrics = NULL,
  control = NULL
)
}
\arguments{
\item{object}{A \code{workflow} object with \code{tune::tune()} placeholders.}

\item{resamples}{An \code{rset} object (e.g. from \code{rsample::vfold_cv()}).}

\item{suggest_fn}{A function \verb{function(trial) -> named list} mapping trial
suggestions to parameter names. Parameter names must match those in the
workflow's tuning parameters.}

\item{n_trials}{Number of trials to run.}

\item{direction}{"minimize" or "maximize".}

\item{sampler}{An roptuna sampler. Defaults to \code{tpe_sampler()}.}

\item{pruner}{An roptuna pruner or NULL.}

\item{storage}{An roptuna storage backend or NULL (in-memory).}

\item{study_name}{Character study name.}

\item{metrics}{A \code{yardstick::metric_set}. Defaults to workflow's default metric.}

\item{control}{A \code{tune::control_resamples()} object.}
}
\value{
An roptuna \code{Study} object. Use \code{study$best_params} to retrieve
the best hyperparameters.
}
\description{
Run a tidymodels workflow using roptuna for hyperparameter search
}
\examples{
\dontrun{
# Requires tune, parsnip, rsample, workflows, yardstick packages
library(tune); library(parsnip); library(rsample); library(workflows)
wf <- workflow() |>
  add_model(decision_tree(cost_complexity = tune()) |> set_engine("rpart") |>
              set_mode("classification")) |>
  add_formula(Species ~ .)
folds <- vfold_cv(iris, v = 3)
study <- tune_optuna(wf, folds,
  suggest_fn = function(trial) list(cost_complexity = trial$suggest_float("cp", 0, 0.1)),
  n_trials = 5, direction = "minimize")
study$best_params
}
}
